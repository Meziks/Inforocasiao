# Configuração com credenciais reais — NUNCA versionar
/config/config.php
/config/google.php

# Cache de avaliações do Google (gerado em runtime)
/storage/reviews.json

# Imagens carregadas pelo gestor no servidor (não pertencem ao código)
/uploads/*
!/uploads/.gitkeep
!/uploads/.htaccess

# Dependências / build
/vendor/
/node_modules/

# Sistema operativo / editores
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
